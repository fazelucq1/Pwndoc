# Config Docker Compose per l'esecuzione locale (prod-local).
# Con questi valori basta `docker compose up -d` per avviare la versione con le tabelle.
COMPOSE_FILE=docker-compose.yml:docker-compose.prod.local.yml
COMPOSE_PROJECT_NAME=pwndoc
APP_PORT=9443
